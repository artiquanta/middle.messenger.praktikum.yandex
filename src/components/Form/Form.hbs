<form class="form" id="{{formId}}" novalidate>
  {{#if heading}}
  {{{heading}}}
  {{/if}}
  <div class="form__inputs">
    {{{formError}}}
    {{#each inputsList}}
    {{{this}}}
    {{/each}}
  </div>
  <button class="form__button" type="submit" disabled>{{buttonTitle}}</button>
  {{#if link}}
  {{{link}}}
  {{/if}}
</form>
